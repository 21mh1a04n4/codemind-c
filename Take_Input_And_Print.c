// take input and output
// you just need to take input from stdin and print it to the stdout.
#include<stdio.h>
int main()
{
    char str[20];
    scanf("%[^
]s",str);
    printf("%s",str);
    return 0;
}