//you just need to take two numbers as input from stdin and you need to multiply them
#include<stdio.h> 
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    int product;
    product=a*b;
    printf("%d",product);
    return 0;
}